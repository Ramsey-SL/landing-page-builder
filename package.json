{
  "name": "landing-page-builder",
  "version": "1.0.0",
  "description": "Strategy Labs — Shopify landing page → Netlify performance rebuild",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "scrape": "node tools/scrape.js",
    "build": "node tools/build.js",
    "audit": "node tools/audit.js",
    "deploy": "node tools/deploy.js",
    "serve": "node tools/serve.js",
    "smoke": "node tools/smoke-test.mjs",
    "monitor": "node tools/monitor.js",
    "run-all": "npm run scrape && npm run build && npm run audit && npm run deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "lighthouse": "^12.0.0",
    "puppeteer": "^23.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
